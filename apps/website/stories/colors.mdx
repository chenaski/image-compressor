import { Meta } from '@storybook/blocks';

<Meta title="Design Tokens/Colors" />

# Colors

<div className={'grid grid-cols-3 gap-8 pt-4'}>
  {[
    'bg-primary',
    'bg-secondary',
    'bg-tertiary',
    'bg-inverted',
    'bg-bgBase',
    'bg-bgPrimary',
    'bg-bgSecondary',
    'bg-bgTertiary',
    'bg-iconPrimary',
    'bg-iconSecondary',
    'bg-iconInverted',
    'bg-strokeFieldDefault',
    'bg-strokeFieldActive',
    'bg-strokeSecondary',
    'bg-white',
    'bg-black',
    'bg-accent',
  ].map((color) => (
    <div key={color} className="flex items-center gap-4">
      <div className={`h-[40px] w-[40px] rounded-full border border-[#0000001F] ${color}`} />
      <div>{color}</div>
    </div>
  ))}
</div>
